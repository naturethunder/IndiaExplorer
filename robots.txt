# IndiaExplore — robots.txt
# Allow full crawl. Destination pages use destination.html?slug=<slug>
# as the canonical URL (listed in sitemap.xml). 2,389 redirect stubs in /stubs/
# redirect each /<slug>.html to the canonical destination.html?slug=<slug>.
User-agent: *
Allow: /
Disallow: /stubs/

Sitemap: https://www.indiaexplore.example/sitemap.xml
